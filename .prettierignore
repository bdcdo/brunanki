# Artefatos gerados pelo pipeline de catálogo: reformatá-los produziria um diff
# gigante a cada `pnpm data:refresh` e esconderia a mudança real de dados.
src/data/catalog.json
src/data/runtime-catalog.json

# O CSS de antes da migração para o Tailwind, congelado byte a byte enquanto a
# camada de cascata `legacy` existir. Reformatá-lo destruiria a única prova de
# que a migração não alterou uma linha de CSS:
#   git show <sha>:src/app/globals.css | diff - src/app/legacy.css
src/app/legacy.css

# Diretórios de build e relatórios
.next/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml
