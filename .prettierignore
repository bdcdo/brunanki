# Artefatos gerados pelo pipeline de catálogo: reformatá-los produziria um diff
# gigante a cada `pnpm data:refresh` e esconderia a mudança real de dados.
src/data/catalog.json
src/data/runtime-catalog.json

# Também gerado, e por um motivo mais duro: `pnpm data:validate` compara o
# arquivo em disco byte a byte com a saída de `buildAttributionMarkdown`. Se o
# Prettier reformatar a tabela de 195 linhas, o gate passa a acusar drift que
# não existe.
docs/atribuicao-de-bandeiras.md

# Diretórios de build e relatórios
.next/
coverage/
playwright-report/
test-results/
pnpm-lock.yaml
